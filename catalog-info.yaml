apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: sbx-techdocs
  description: "RH Dev Hub"
  annotations:
    # this could also be `url:<url>` if the documentation isn't in the same location
    backstage.io/techdocs-ref: dir:.
  tags:
    - techdocs
    - rhdh
    - ibm-redhat
spec:
  type: documentation
  lifecycle: experimental
  owner: dev-hub-team
  system: Architecture
